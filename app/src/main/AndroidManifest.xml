<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.canvasapplicatioon">
    <!-- 🔧 вот это нужно добавить -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CanvasApplicatioon"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".activities.StudentCourseDetailActivity"
            android:exported="false" />
        <activity
            android:name=".activities.StudentCoursesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CourseDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.TeacherCoursesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AdminCoursesActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AdminUsersActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AdminPanelActivity"
            android:exported="false" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>